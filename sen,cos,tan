from math import sin, cos, tan, radians
n1=float(input('Digite o valor de um angulo:'))
rad=radians(n1)
print(f'O seno, cosseno e tangente deste angulo, valem respectivamente: {sin(rad):.2f}, {cos(rad):.2f} e {tan(rad):.2f}')
